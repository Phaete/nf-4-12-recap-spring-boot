package com.phaete.nf412recapspringboot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Nf412RecapSpringBootApplication {

    public static void main(String[] args) {
        SpringApplication.run(Nf412RecapSpringBootApplication.class, args);
    }

    /*
    Bonus: Monitoring

    Monitoring can be useful in case the server is under heavy load.
    It can show vulnerabilities or other problems with the server that can potentially be exploited by attackers or
    users who just happen to stumble into it.
     */

}
